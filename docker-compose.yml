version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: telegram-notes-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./notes:/app/notes
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"