version: '3.8'

services:
  notes_bot:
    build: .
    container_name: notes_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ${OBSIDIAN_VAULT_PATH}:/notes:rw
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
